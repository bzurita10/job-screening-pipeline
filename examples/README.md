# examples/

Generated resumes and cover letters are written to `output/` at runtime (which
is git-ignored, since those documents contain your personal contact details).
Nothing personal is committed to this repo.

To see the tool produce a document, add your own `profile.yaml` (copy
`profile.example.yaml`) and run the pipeline on one of the sample postings in
`jobs/`.
